export const newBook = (title, imgRef, desc, progress) => {
    var title = document.createElement('h1');
    var desc = document.createElement('div');
    var imgRef = document.createElement('img');
}